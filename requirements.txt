# Pinned to known-good versions for production reproducibility.
# - `~=X.Y.Z` (compatible release) allows patch updates within the same minor
#   version, picking up security fixes without surprise API breaks.
# - `==X.Y.Z` (exact) is used for fast-moving pre-1.0 deps where the API
#   surface still moves between minor releases.
# Bump deliberately; don't auto-upgrade.

streamlit~=1.55.0
langgraph==1.0.7
requests~=2.32.5
pyodbc~=5.3.0
rapidfuzz~=3.14.3
python-dotenv~=1.2.1
msal~=1.36.0
pandas~=2.3.3
